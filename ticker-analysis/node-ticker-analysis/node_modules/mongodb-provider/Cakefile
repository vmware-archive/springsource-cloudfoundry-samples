{exec} = require "child_process"

task "build", ->
  exec "coffee -c ./", (err)->
    console.log err.stack if err

task 'test', ->
  invoke 'build'
  exec 'coffee ./examples/queries.coffee', (err)->
    console.log err.stack if err
